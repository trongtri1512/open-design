version: '3.8'

services:
  open-design:
    build: .
    container_name: open-design
    volumes:
      - ~/.open-design:/root/.open-design
    ports:
      - "3030:3030"
      - "7456:7456"
    restart: unless-stopped
